{
	"name": "@zaydek/sorcery",
	"version": "0.4.2",
	"main": "dist/index.css",
	"license": "MIT",
	"files": [
		"dist/"
	],
	"scripts": {
		"build-readable": "yarn -s node-sass --indent-type tab --indent-width 1 src/sass/index.scss > dist/index.css && yarn prettier -w dist/index.css",
		"build-minified": "node-sass --output-style compressed src/sass/index.scss > dist/index.min.css",
		"test": "jest",
		"test-watch": "jest --watch",
		"build": "yarn test && rm -rf dist && yarn tsc && yarn build-readable && yarn build-minified",
		"publish-dry-run": "yarn build && npm publish --dry-run"
	},
	"devDependencies": {
		"@types/jest": "^26.0.15",
		"jest": "^26.6.1",
		"node-sass": "^4.14.1",
		"prettier": "^2.1.2",
		"ts-jest": "^26.4.3",
		"typescript": "^4.0.5"
	}
}
